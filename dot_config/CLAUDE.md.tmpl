# Chezmoi Configuration Directory

{{ if eq .chezmoi.os "darwin" -}}
**This host:** macOS — work laptop (`{{ .chezmoi.hostname }}`). Package manager: **Homebrew** (`brew`). This is **NOT** Arch and **NOT** Ubuntu — never use `pacman` or `apt`.
{{- else if eq .chezmoi.osRelease.id "arch" -}}
**This host:** Arch Linux (`{{ .chezmoi.hostname }}`) — my default environment. Package manager: **pacman** (+ AUR).
{{- else if eq .chezmoi.osRelease.id "ubuntu" -}}
**This host:** Ubuntu — work desktop (`{{ .chezmoi.hostname }}`). Package manager: **apt**. This is **NOT** Arch — never use `pacman`.
{{- else -}}
**This host:** {{ .chezmoi.os }}{{ with .chezmoi.osRelease.id }} / {{ . }}{{ end }} (`{{ .chezmoi.hostname }}`). Distro unrecognized by this template — verify the package manager (`pacman`/`apt`/`brew`) before using one.
{{- end }}

> This file is synced by chezmoi across several machines (Arch, Ubuntu, macOS). The host line above is rendered from `.chezmoi` facts — trust it over any distro assumptions you bring from skills, memory, or other dotfiles in this repo.

Many files in this directory and its subdirectories are managed by chezmoi.

{{ if eq .chezmoi.os "linux" -}}
## Compositor

The active Wayland compositor is **niri** (`~/.config/niri/config.kdl`), the daily driver as of July 2026. Apply compositor/keybinding changes to the niri config only.

The previous driver was **sway** (`~/.config/sway/config`, a chezmoi template). Its config is kept around because I may ask you to carry settings or keybindings over from it to niri — but do not edit it otherwise. The Hyprland config (`~/.config/hypr/hyprland.conf`) is **vestigial and unused** — do not edit it.

{{ end -}}
## Before Editing Files

Check if a file is managed by chezmoi:
```bash
chezmoi source-path <file>
```

If the file is managed by a **template** (source file ends in `.tmpl`), do not edit the target file directly. Instead, edit the template in the chezmoi source directory.

## After Editing

Run `/chezmoi-sync` after:
- Editing any chezmoi-managed file
- Creating new files in directories where other files are managed by chezmoi

## Before Pushing to GitHub

Always check with me before pushing chezmoi changes to GitHub — **even in auto/autonomous mode**. Staging the changes (`chezmoi re-add`/`chezmoi add`) and committing locally is fine, but pause and ask for explicit confirmation before `git push`. I often iterate rapidly, and I don't want a string of pushed commits representing states that were immediately discarded.
