# Ubuntu-only brave wrapper (real brave binary on Arch shouldn't be shadowed)
.local/bin/brave

# Claude Code per-project state (auto-memory, transcripts) — machine-local, keep out of dotfiles
.claude/projects

# Per-host wezterm override files (opacity, font size) — never manage these
.config/wezterm/*.local

# Per-host kitty override file (globinclude'd last from kitty.conf) — never manage
.config/kitty/kitty.local.conf
